[project]
name = "real-estate-amenities-price-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TruongHaiTrieu", email = "truonghaitrieu1805@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.3",
    "bs4>=0.0.2",
    "geopy>=2.5.0",
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
real-estate-amenities-price-analysis = "real_estate_amenities_price_analysis:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
